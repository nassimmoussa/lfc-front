import React from 'react';

const ChainedIfPreview = ({ lE }) => {
  return (
    <div>
      <h2>ChainedIfPreview</h2>
      <h2>{JSON.stringify(lE)}</h2>
    </div>
  );
};

export default ChainedIfPreview;
